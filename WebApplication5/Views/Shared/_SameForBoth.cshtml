@model List<int>
<div class="row">
    <div class="col-md-6 col-md-offset-3 well">
        <h1>HELLO FROM THE PARTIAL!!!</h1>
        <ul>
            @foreach (int num in Model)
            {
                <li>@num</li>
            }
        </ul>
    </div>
</div>